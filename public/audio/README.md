# Audio

- `sfx/` — Kenney "Interface Sounds" (CC0, public domain, no attribution required).
- `announcer/` — generated by `npm run announcer` via ElevenLabs "stadium voice".
  Every spoken number is fact-checked against the real stats at build time.
- `theme.mp3` — **not present.** Drop the BattleBots theme here and it is picked
  up automatically (fades in on first interaction, toggle bottom-right).
  The file supplied as `battlebots_themesong.mp4` was a failed download:
  9 bytes containing the ASCII text "Not Found".
